PyHPP API Documentation (Markdown)
==================================

This page embeds the Markdown source file.

If you want *rendered* Markdown (headings, links, tables), install MyST and rebuild:

.. code-block:: bash

   python3 -m pip install --user myst-parser
   make clean html

Source:

.. literalinclude:: pyhpp_api_documentation.md
   :language: text
